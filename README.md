# Amy's Plugin Dependencies

This repository's only purpose is to provide external files needed by revolt plugins.

## Sketchvolt

Requirements:
- sketchpad.js (Canvas functionality)
- sketchpadTemplate.js (HTML template)